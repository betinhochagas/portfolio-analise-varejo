# Ambiente virtual Python
.venv/
venv/
__pycache__/
*.pyc

# Jupyter
.ipynb_checkpoints/

# Banco de dados (gerado pelo ETL - pode ser recriado)
# Comente a linha abaixo se quiser versionar o .db
# dados/tratado/varejo.db

# Sistema
.DS_Store
Thumbs.db

# Power BI temporarios
*.pbix.tmp

# dbt (artefatos gerados)
dbt/target/
dbt/dbt_packages/
dbt/logs/
dbt/profiles.yml

# Credenciais / chaves de servico (NUNCA versionar)
*-key.json
*_key.json
service-account*.json

# Notas internas de sessao (nao publicar)
_HANDOFF_STATUS.md
